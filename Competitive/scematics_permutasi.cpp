#include <bits/stdc++.h>
using namespace std;

#define f(a,b) for(int i = a; i < b; i++)
#define pb push_back

int main()
{
	vector<int> v;
	vector<int> temp; 
	int t,val;
	cin >> t;
	while(t--){
		int n; 
		cin >> n;
		f(0,n){
			cin >> val;
			v.pb(val);
			temp.pb(v[i]);
		}
		sort(temp.begin(),temp.end());
		while(found){
			for(int i = 0; i < n; i++){
				
			}
		}
	}

	return 0;
}

